# WorldCore Python runtime dependencies
# Add packages here only when the project actually needs external dependencies.
# Tkinter is part of the Python/Tcl-Tk install and should not be installed with pip.
